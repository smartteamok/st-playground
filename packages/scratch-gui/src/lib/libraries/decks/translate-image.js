/**
 * Tutorial step images were removed from ST-Playground. Keep the helper so
 * cards still import it.
 */
const loadImageData = () => {};
const translateImage = () => null;

export {
    loadImageData,
    translateImage
};
